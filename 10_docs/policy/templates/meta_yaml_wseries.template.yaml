# Project metadata for {REPO_ID}
# Auto-generated by Claude - agents should keep this current
# Template: C010_standards/10_docs/policy/templates/meta_yaml_wseries.template.yaml
# Spec: C010_standards/protocols/tier2_wseries_spec.md

project:
  last_reviewed: {TODAY}           # REQUIRED: Date META.yaml was last verified
  summary: "{SUMMARY}"             # REQUIRED: One-line description (under 100 chars)
  status: active                   # REQUIRED: active | stale | experimental | archived
  series: W                        # REQUIRED: W for work projects
  client: "{CLIENT}"               # REQUIRED for W-series: Client name (e.g., Wowway, Internal)

folders:
  # REQUIRED: Document all top-level folders
  10_docs: "Documentation and guides"
  20_receipts: "Change receipts and evidence"
  # Add other folders as they exist in this repo

files:
  # REQUIRED: Document key files
  CLAUDE.md: "Claude Code guidance"
  README.md: "Project documentation"
  # Add other key files (Makefile, main scripts, etc.)

relates_to:
  # OPTIONAL: Document meaningful cross-project dependencies
  # - W005_BigQuery: "Data source"
  # - C001_mission-control: "Credential access"

maintainer_notes:
  # OPTIONAL: Important operational notes, gotchas, tips
  # - "Run reports weekly on Mondays"
  # - "Requires BigQuery credentials in vault"
