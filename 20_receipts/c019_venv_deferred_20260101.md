# C019 venv Audit Issue - Deferred Fix
## Date: 2026-01-01

## Status
**DEFERRED** - Active session using venv, do not delete.

## Current State
- `venv/` exists at top-level (audit violation)
- NOT tracked by git (0 files) ✓
- In `.gitignore` ✓
- Added `.stignore` to prevent Syncthing re-sync

## Root Cause
Syncthing was syncing `venv/` from another node. When deleted, it was recreated either by:
1. Syncthing restoring from another device
2. A script rebuilding it automatically

## Durable Fix (After C019 Session Ends)

### Option A: External venv (Preferred)
```bash
# Create venv outside repo
python -m venv ~/.venvs/c019-docs-site

# Update scripts to use external venv
# Edit serve_docs.command or similar to reference:
#   ~/.venvs/c019-docs-site/bin/python

# Delete repo-local venv
rm -rf /Users/jeremybradford/SyncedProjects/C019_docs-site/venv/
```

### Option B: Relocate Under Allowed Dir
```bash
# Move to 00_run/ (allowed top-level dir)
cd /Users/jeremybradford/SyncedProjects/C019_docs-site
rm -rf venv/
python -m venv 00_run/venv

# Update scripts to use 00_run/venv/bin/python
# Add 00_run/venv/ to .gitignore if not already covered
```

## Syncthing Fix Applied
Created `.stignore` with:
```
venv/
.venv/
__pycache__/
*.pyc
node_modules/
.DS_Store
```

This prevents future Syncthing re-creation across devices.

## Audit Impact
- C019 temporarily shows as non-compliant
- Will auto-resolve once venv is relocated/removed
- No action needed on other devices

## Session Attribution
Generated by Claude Code on 2026-01-01
