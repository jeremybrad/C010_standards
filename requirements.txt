# Core dependencies for C010_standards validators
# Install: pip install -r requirements.txt

# YAML parsing for DocMeta, CodeMeta, and taxonomy validation
PyYAML>=6.0

# JSON schema validation for Houston features config
jsonschema>=4.17.0

# Development dependencies (optional)
# Install with: pip install -r requirements-dev.txt

